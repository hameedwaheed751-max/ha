# دليل تشخيص الشبكة - NetAgent

## 🔧 الميزات الجديدة

### زر تشخيص الاتصال
تم إضافة زر جديد في Dashboard (أيقونة 📊 في الزاوية العلوية اليسرى):
- اضغط على الزر لعرض معلومات الاتصال بخادم SAS
- يعرض: URI الكامل، scheme، host، port، authority

### إعادة محاولة تلقائية
- عند حدوث خطأ اتصال ("Connection reset by peer")
- سيحاول البرنامج إعادة الاتصال تلقائياً (حتى 2 مرات)
- تأخير متزايد بين المحاولات (2، 4 ثوان)

### معلومات تشخيصية محسّنة
- جميع طلبات GET و POST تُطبع في console
- تتبع مفصّل لكل خطوة من خطوات الاتصال
- معلومات التحقق من البيانات والرموز

## 🔍 كيفية التشخيص

### 1. التحقق من الاتصال الأساسي
```
Dashboard → زر التحليلات (📊) → اقرأ معلومات URI
```

تأكد من:
- `scheme` يجب أن يكون: `https` أو `http`
- `port` يجب أن يكون: `443` (للـ HTTPS) أو `80` (للـ HTTP)
- `host` يطابق عنوان SAS المدخل في الإعدادات

### 2. فحص رسائل الخطأ
عند حدوث خطأ:
- 🔄 (Retry) = محاولة إعادة الاتصال
- ⚠️ (Connection error) = فشل الاتصال بالخادم
- ❌ (Error) = رد SAS غير متوقع

### 3. مراجعة إعدادات SAS
انتقل إلى: الإعدادات → الاتصال بـ SAS

تحقق من:
- **عنوان الخادم**: يجب أن يكون بدون منفذ (مثل `https://sas.speednet-iq.com`)
- **إسم المستخدم**: استخدم حساب له صلاحيات إدارة
- **كلمة المرور**: تأكد من صحتها

## 📊 رسائل الخطأ الشائعة

| الخطأ | المعنى | الحل |
|-------|--------|------|
| Connection reset by peer | قطع الاتصال من الخادم | تحقق من وجود الإنترنت وعنوان الخادم |
| HTTP 403 | لا توجد صلاحيات | تحقق من بيانات تسجيل الدخول والصلاحيات |
| HTTP 401 | بيانات تسجيل الدخول خاطئة | أعد إدخال اسم المستخدم وكلمة المرور |
| HTTP 404 | المورد غير موجود | تحقق من صحة عنوان الخادم |
| Timeout | الخادم بطيء جداً | تحقق من سرعة الإنترنت |

## 🛠️ خطوات الحل السريع

1. **إعادة تشغيل التطبيق**
   ```
   إغلاق وفتح التطبيق من جديد
   ```

2. **مسح بيانات الجلسة**
   ```
   الإعدادات → مسح البيانات
   ```

3. **إعادة تعيين الاتصال**
   ```
   الإعدادات → الاتصال بـ SAS → إدخال البيانات مرة أخرى
   ```

4. **التحقق من الشبكة**
   ```
   تأكد من اتصالك بالإنترنت
   افتح متصفح وحاول الوصول لعنوان SAS
   ```

## 📝 معلومات تقنية

### توقيتات الاتصال (Timeouts)
- GET requests: 45 ثانية
- POST requests: 60 ثانية
- كل محاولة إعادة تأخذ نفس التوقيت

### إعادة المحاولة (Retries)
- عدد المحاولات: 2 + محاولة أولى = 3 محاولات إجمالية
- التأخير بين المحاولات: 2-4 ثوانٍ
- الأخطاء المُعاد محاولتها: SocketException فقط

### معايير النجاح
- Status Code: 200-299 للـ GET، 200-399 للـ POST
- Response Body: JSON صحيح ويحتوي على `status` أو `success`

## ℹ️ الحصول على المساعدة

إذا استمرت المشاكل:
1. اضغط على زر التشخيص واحفظ المعلومات
2. افتح console (Ctrl+Shift+I في المتصفح) واحفظ رسائل الأخطاء
3. تواصل مع الدعم مع:
   - معلومات التشخيص
   - رسائل الخطأ المحددة
   - عنوان الخادم (بدون كلمة السر)
